package com.example.tanlam.controller.notification

class NotificationId {
    companion object {
        const val NOTIFICATION_ORDER = "Order's notification"
        const val NOTIFICATION_CHECK = "Check's notification"
    }
}